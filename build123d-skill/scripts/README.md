# Build123d Scripts

Put deterministic build123d validators, exporters, or inspection helpers here when repeated CAD checks become mechanical enough to automate.

Scripts should print actionable validation output and avoid changing user files unless the calling instructions say so.
